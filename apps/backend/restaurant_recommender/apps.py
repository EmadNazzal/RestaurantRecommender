"""This file contains the configuration for the restaurant_recommender app."""

from django.apps import AppConfig


class RestaurantRecommenderConfig(AppConfig):
    """Configuration for the restaurant_recommender app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "restaurant_recommender"
